# Local kind development placeholder only — not a production credential.
# Matches .env.example and docker-compose.yml placeholder values.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  namespace: krp
type: Opaque
stringData:
  POSTGRES_USER: app_user
  POSTGRES_PASSWORD: change_me
  POSTGRES_DB: k8s_reliability
